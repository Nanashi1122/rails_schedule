<%= render "shared/header" %>

<% if flash[:notice_N] %>
        <p class="notice"><%= flash[:notice_N] %></p>
<% end %>

<%= render "shared/error", obj: @schedule %>

<div class="title-container">
    <h3>スケジュール編集</h3>
</div>



<%= form_with model: @schedule do |f| %>

    <table>
        <tr>
            <th><%= f.label :title ,"タイトル" %></th>
            <td><%= f.text_field :title, :size=>"30" %></td>
        </tr>
    
        <tr>
            <th><%= f.label :start_day ,"開始日" %></th>
            <td><%= f.date_field :start_day %></td>
        </tr>
    
        <tr>
            <th><%= f.label :end_day ,"終了日" %></th>
            <td><%= f.date_field :end_day %></td>
        </tr>
    
        <tr>
            <th><%= f.label :all_day ,"終日" %></th>
            <td><%= f.check_box :all_day %>終日の場合はチェック</td>
        </tr>

        <tr>
            <th><%= f.label :memo, "メモ" %>
            <td><%= f.text_area :memo, :size=>"50" %>
        </tr>
    </table>
 
    <div class="edit-bottom">
        <ul>
            <li><%= f.submit "編集完了する" %></li>
            <li><%= link_to "スケジュール一覧に戻る", :schedules %></li>
        </ul>
    </div>
<% end %>